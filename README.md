## Quickstart

```bash
# You probably want to do this in a dedicated enviroment
pip install -r requirements.txt

# Build the static site into dist
jupyter lite build

# Preview locally
jupyter lite serve
```
